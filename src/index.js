import React from 'react';
import ReactDOM from 'react-dom';
import reportWebVitals from './reportWebVitals';

import RickAPI from '../src/RickAPI';

ReactDOM.render(
  <React.StrictMode>
    <RickAPI />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
